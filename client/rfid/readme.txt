readCard() 		// Configures the rfid reader and opens the serialport,     [kutsu t�t�]
writeData()  		// then connects to writeData() which reads the rfid tag
QString cardID;		// and returns QString cardID;
