DLLMySQL::specificEvents(QString dateStart, QString dateEnd);
- Call this function in the executable with parameters in format "YYYY-MM-DD"
- FYI: dateStart and dateEnd can't be same for showing all events in one day, dateEnd has to be
the following date